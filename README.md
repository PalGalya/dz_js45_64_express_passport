# Express Server з Mongoose ORM та Passport авторизацією

> **71: Інтеграція Mongoose для повнофункціонального fullstack проекту**

## 📋 Опис проекту

Це повнофункціональний Express сервер з інтеграцією **Mongoose ORM** для роботи з MongoDB, що включає:

- 🔐 **Авторизацію** через Passport.js з локальною стратегією
- 📦 **CRUD операції** для користувачів та продуктів
- ✅ **Валідацію** даних через Celebrate + Joi
- 🗄️ **Mongoose моделі** з валідацією та індексами
- 🔒 **Захищені маршрути** для авторизованих користувачів

---

## 🎯 Мета завдання

1. ✅ **Рефакторинг серверної частини**: Переробити backend для використання **Mongoose** замість raw MongoDB драйвера
2. ✅ **Розробка API**: Розробити/оптимізувати API для забезпечення зв'язку з frontend частиною на React
3. ✅ **Тестування функціональності**: Перевірити всі функції працюють коректно після рефакторингу
4. ✅ **MongoDB Mongoose**: Використовувати **Mongoose як інструмент ORM** для MongoDB

---

## 🛠️ Технічні вимоги

### 1. Розробка схем даних

- ✅ Створити схеми Mongoose для моделювання всіх потрібних даних у проекті
- ✅ Включити валідацію та індексацію для забезпечення інтеграції та продуктивності даних

### 2. Рефакторинг доступу до бази даних

- ✅ Переписати CRUD операції, використовуючи моделі Mongoose для підвищення ефективності та безпеки доступу до даних

### 3. Тестування функціональності

- ✅ Переконатися, що всі існуючі функції працюють коректно після рефакторингу
- ✅ Провести тестування взаємодії між backend на Mongoose та frontend на React

---

## 📦 Залежності

```json
{
  "express": "^5.1.0",
  "mongoose": "^8.0.0",
  "passport": "^0.7.0",
  "passport-local": "^1.0.0",
  "express-session": "^1.17.3",
  "celebrate": "^15.0.3",
  "joi": "^18.0.1",
  "dotenv": "^17.2.3"
}
```

---

## 📁 Структура проекту

```
src/
├── app.mjs                          # Головний файл додатку
├── config.mjs                       # Конфігурація (вимагає .env)
├── index1.mjs                       # Додатковий файл
│
├── auth/
│   └── passport.mjs                 # Конфігурація Passport.js з Mongoose
│
├── controllers/
│   ├── auth.mjs                     # Контролери для авторизації
│   └── products.mjs                 # Контролери для продуктів (CRUD)
│
├── middleware/
│   └── auth.mjs                     # Middleware перевірки авторизації
│
├── routes/
│   ├── auth.mjs                     # Маршрути авторизації
│   ├── index.mjs                    # Основний роутер
│   └── products.mjs                 # Маршрути продуктів (CRUD)
│
├── models/
│   ├── User.mjs                     # Mongoose модель користувача
│   └── Product.mjs                  # Mongoose модель продукту
│
├── db/
│   └── mongodb.mjs                  # Підключення до MongoDB з Mongoose
│
└── public/
    └── products.html                # Статична HTML сторінка
```

---

## 🔑 Mongoose Моделі

### User Model

```javascript
{
  email: String (unique, required, email validation),
  password: String (required, min 6 chars),
  id: String (unique, required),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

### Product Model

```javascript
{
  id: Number (unique, required),
  name: String (required, min 3 chars),
  description: String (required),
  price: Number (required, min 0),
  category: String (enum validation),
  image: String (optional),
  stock: Number (default 0),
  rating: Number (0-5),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

---

## 🚀 API Endpoints

### 🔐 Авторизація

| Метод | Маршрут           | Опис                          | Авторизація |
| ----- | ----------------- | ----------------------------- | ----------- |
| POST  | `/auth/register`  | Реєстрація нового користувача | ❌          |
| POST  | `/auth/login`     | Вхід в систему                | ❌          |
| POST  | `/auth/logout`    | Вихід з системи               | ✅          |
| GET   | `/auth/me`        | Дані поточного користувача    | ✅          |
| GET   | `/auth/protected` | Захищена сторінка (демо)      | ✅          |

### 📦 Продукти (CRUD)

| Метод  | Маршрут             | Опис                   | Авторизація |
| ------ | ------------------- | ---------------------- | ----------- |
| GET    | `/api/products`     | Отримати всі продукти  | ❌          |
| GET    | `/api/products/:id` | Отримати продукт за ID | ❌          |
| POST   | `/api/products`     | Створити продукт       | ✅          |
| PUT    | `/api/products/:id` | Оновити продукт        | ✅          |
| DELETE | `/api/products/:id` | Видалити продукт       | ✅          |

---

## 📝 Приклади запитів

### Реєстрація

```bash
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

### Вхід

```bash
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }' \
  -c cookies.txt
```

### Отримати поточного користувача

```bash
curl -X GET http://localhost:3000/auth/me \
  -b cookies.txt
```

### Отримати всі продукти

```bash
curl -X GET http://localhost:3000/api/products
```

### Створити продукт (потребує авторизації)

```bash
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{
    "name": "Ноутбук",
    "price": 25000,
    "description": "Потужний ноутбук для роботи",
    "category": "Ноутбуки"
  }'
```

### Оновити продукт (потребує авторизації)

```bash
curl -X PUT http://localhost:3000/api/products/1 \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{
    "price": 30000,
    "stock": 10
  }'
```

### Видалити продукт (потребує авторизації)

```bash
curl -X DELETE http://localhost:3000/api/products/1 \
  -b cookies.txt
```

---

## 🔧 Встановлення та запуск

### Передумови

- Node.js 18+
- npm або yarn
- MongoDB Atlas аккаунт (або локальна MongoDB)

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/PalGalya/dz_js31_48.git
cd dz_js31_48
git checkout dz_js52_71_feature_mongoose-orm-integration
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

### Крок 3: Налаштування .env

Створіть файл `.env` у корені проекту:

```env
PORT=3000
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=expressApp
SESSION_SECRET=your-secret-key-change-in-production
```

### Крок 4: Запуск сервера

**Режим розробки (з автоперезавантаженням):**

```bash
npm run dev
```

**Режим продакшену:**

```bash
npm start
```

Сервер буде доступний на `http://localhost:3000`

---

## 👤 Тестові користувачі

Тестові користувачі можна зареєструвати через API. Приклад для Postman/Insomnia:

```json
{
  "email": "test@example.com",
  "password": "password123"
}
```

---

## 🔒 Безпека

⚠️ **Важливо:** Це навчальний проект. Для продакшену потрібно:

- ✅ **Хешування паролів** - Використовувати `bcrypt` або `argon2`
- ✅ **Шифрування з'єднання** - Використовувати HTTPS (secure cookies)
- ✅ **Безпечні змінні оточення** - Приховувати всі секретні ключі
- ✅ **CORS** - Налаштувати правильно для фронтенду
- ✅ **Rate limiting** - Обмежити кількість запитів
- ✅ **SQL/NoSQL Injection** - Mongoose захищає від цього автоматично

---

## 📊 Валідація даних

### Через Celebrate + Joi

```javascript
// Реєстрація
{
  email: string, email format, required
  password: string, min 6 chars, required
}

// Продукт
{
  name: string, min 3-max 100, required
  price: number, min 0, required
  description: string, min 10-max 500, required
  category: enum (specific categories), required
}
```

---

## 🧪 Тестування

### Перевірити підключення до БД

```bash
curl http://localhost:3000/auth/me
```

Має повернути 401 (без авторизації)

### Тестування авторизації

1. Реєстрація → Отримати ID користувача
2. Вхід → Отримати session cookie
3. Доступ до `/auth/me` → Повинен працювати
4. Вихід → Видалити session

### Тестування CRUD продуктів

1. GET `/api/products` → Має показувати тестові продукти
2. POST `/api/products` → Потребує авторизації
3. PUT `/api/products/:id` → Оновити дані
4. DELETE `/api/products/:id` → Видалити продукт

---

## 📈 Структура відповідей

### Успішна відповідь

```json
{
  "status": "success",
  "message": "Операція виконана успішно",
  "data": {
    "user": {
      "id": "1",
      "email": "user@example.com"
    }
  }
}
```

### Помилка

```json
{
  "status": "error",
  "message": "Опис помилки"
}
```

---

## 🔄 Важливі файли для рефакторингу

✅ **Переробленi з raw MongoDB на Mongoose:**

- `src/models/User.mjs` - Нова User модель з валідацією
- `src/models/Product.mjs` - Нова Product модель з валідацією
- `src/auth/passport.mjs` - Оновлено для роботи з Mongoose
- `src/controllers/auth.mjs` - Переписано на Mongoose
- `src/controllers/products.mjs` - Переписано на Mongoose
- `src/db/mongodb.mjs` - Оновлено для підтримки Mongoose
- `src/routes/products.mjs` - Додано PUT та DELETE маршрути

---

## 🎓 Навчальні цілі

По завершенню цього проекту ви будете розуміти:

- ✅ Як використовувати **Mongoose** як ORM для MongoDB
- ✅ Як створювати **валідовані схеми** з Mongoose
- ✅ Як реалізувати **CRUD операції** з Mongoose
- ✅ Як інтегрувати **Passport.js** з Mongoose
- ✅ Як будувати **REST API** з Express + Mongoose
- ✅ Як захищати **маршрути через авторизацію**
- ✅ Як валідувати **дані на серверу** з Celebrate

---

## 📚 Посилання

- [Mongoose Документація](https://mongoosejs.com/)
- [Express.js](https://expressjs.com/)
- [Passport.js](http://www.passportjs.org/)
- [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
- [Celebrate Documentation](https://github.com/arb/celebrate)

---

## 📝 Автор

Паламарчук Галина

